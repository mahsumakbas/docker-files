services:
  registry:
    image: registry:latest
    container_name: docker-registry
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - /var/lib/registry:/var/lib/registry
    networks:
      - my_internal
networks:
  my_internal:
    external: true